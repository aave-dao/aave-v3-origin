diff -ruN ../contracts/instances/ATokenInstance.sol contracts/instances/ATokenInstance.sol
--- ../contracts/instances/ATokenInstance.sol	2025-07-06 19:25:57.568302476 +0300
+++ contracts/instances/ATokenInstance.sol	2025-07-13 11:26:19.044856577 +0300
@@ -47,15 +47,15 @@
 
     _domainSeparator = _calculateDomainSeparator();
 
-    emit Initialized(
-      underlyingAsset,
-      address(POOL),
-      address(TREASURY),
-      address(REWARDS_CONTROLLER),
-      aTokenDecimals,
-      aTokenName,
-      aTokenSymbol,
-      params
-    );
+    //    emit Initialized(
+    //underlyingAsset,
+    //address(POOL),
+    //address(TREASURY),
+    //address(REWARDS_CONTROLLER),
+    //aTokenDecimals,
+    //aTokenName,
+    //aTokenSymbol,
+    //params
+    //);
   }
 }
diff -ruN ../contracts/protocol/tokenization/base/ScaledBalanceTokenBase.sol contracts/protocol/tokenization/base/ScaledBalanceTokenBase.sol
--- ../contracts/protocol/tokenization/base/ScaledBalanceTokenBase.sol	2025-07-06 19:25:57.568302476 +0300
+++ contracts/protocol/tokenization/base/ScaledBalanceTokenBase.sol	2025-07-13 11:26:19.044856577 +0300
@@ -36,7 +36,7 @@
   }
 
   /// @inheritdoc IScaledBalanceToken
-  function scaledBalanceOf(address user) external view override returns (uint256) {
+  function scaledBalanceOf(address user) public view override returns (uint256) {
     return super.balanceOf(user);
   }
 
diff -ruN ../.gitignore .gitignore
--- ../.gitignore	1970-01-01 02:00:00.000000000 +0200
+++ .gitignore	2025-07-13 11:26:19.048856636 +0300
@@ -0,0 +1,2 @@
+*
+!.gitignore
