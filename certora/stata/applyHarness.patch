diff -ruN .gitignore .gitignore
--- .gitignore	1970-01-01 02:00:00.000000000 +0200
+++ .gitignore	2025-07-02 13:19:44.683772883 +0300
@@ -0,0 +1,2 @@
+*
+!.gitignore
\ No newline at end of file
diff -ruN src/contracts/extensions/stata-token/ERC20AaveLMUpgradeable.sol src/contracts/extensions/stata-token/ERC20AaveLMUpgradeable.sol
--- src/contracts/extensions/stata-token/ERC20AaveLMUpgradeable.sol	2025-07-02 13:20:01.232056816 +0300
+++ src/contracts/extensions/stata-token/ERC20AaveLMUpgradeable.sol	2025-07-02 13:19:44.683772883 +0300
@@ -151,7 +151,7 @@
   }
 
   ///@inheritdoc IERC20AaveLM
-  function rewardTokens() external view returns (address[] memory) {
+  function rewardTokens() public view returns (address[] memory) {
     ERC20AaveLMStorage storage $ = _getERC20AaveLMStorage();
     return $._rewardTokens;
   }
diff -ruN src/contracts/instances/ATokenInstance.sol src/contracts/instances/ATokenInstance.sol
--- src/contracts/instances/ATokenInstance.sol	2025-07-02 13:20:01.232056816 +0300
+++ src/contracts/instances/ATokenInstance.sol	2025-07-02 13:19:44.683772883 +0300
@@ -47,15 +47,15 @@
 
     _domainSeparator = _calculateDomainSeparator();
 
-    emit Initialized(
-      underlyingAsset,
-      address(POOL),
-      address(TREASURY),
-      address(REWARDS_CONTROLLER),
-      aTokenDecimals,
-      aTokenName,
-      aTokenSymbol,
-      params
-    );
+    //emit Initialized(
+    //underlyingAsset,
+    //address(POOL),
+    //address(TREASURY),
+    //address(REWARDS_CONTROLLER),
+    //aTokenDecimals,
+    //aTokenName,
+    //aTokenSymbol,
+    //params
+    //);
   }
 }
